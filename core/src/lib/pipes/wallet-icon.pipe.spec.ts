import {WalletIconPipe} from './wallet-icon.pipe';

describe('WalletIconPipe', () => {
  it('create an instance', () => {
    const pipe = new WalletIconPipe();
    expect(pipe).toBeTruthy();
  });
});
